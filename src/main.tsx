import React from 'react'
import ReactDOM from 'react-dom/client'
import FormAuth from './modules/Card/FormAuth/FormAuth'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <FormAuth/>
  </React.StrictMode>,
)
